# 占位文件，别删
#
# 为什么需要它：main.go 里有 //go:embed all:frontend/dist，而 .gitignore 忽略 frontend/dist/* 的内容。
# 没有这个文件，clone 下来就没有 dist 目录，go build 会直接报
# "pattern all:frontend/dist: no matching files"（`go test ./...` 也一样）。
#
# 这个文件有两份，内容**必须保持一致**，各管一头：
#   - frontend/public/gitkeep —— 源头。Vite 会把 public/ 原样复制进 dist/，
#     所以每次 npm run build 之后它会自动出现在 dist/ 里，不需要手动恢复
#   - frontend/dist/gitkeep   —— 仓库里那一份，保证"刚 clone、还没跑过前端构建"的人也能直接编译
# 两份内容不一致的话，每次构建都会在 dist/ 里留下一个 diff。
