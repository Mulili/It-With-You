# 构建产物
build/bin
frontend/dist/*
# frontend/dist 需要保留占位文件，否则 main.go 的 //go:embed all:frontend/dist 会编译失败
!frontend/dist/gitkeep

# 依赖
node_modules

# 本地环境变量（含 API Key），绝不提交
.env

# 本地构建缓存（可随时删除，仅为沙箱内构建 Go / npm 而设）
.cache/

# 测试图片不必上传
frontend/src/assets

# 
operation.md

#
TTS原生go方案.md